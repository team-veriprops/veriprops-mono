"""System configuration domain (PRD §18.5, partial — D28).

A minimal admin-editable key-value store for the §14 operational knobs (dispute window,
re-check pricing, agent dispute-defence window). Full Mission-Control config is Phase 18/S22;
this pulls the config store forward, mirroring the D14 trust-weights CRUD.
"""
