{
  "installCommand": "pip install -r requirements.txt",
  "buildCommand": "export veriprops_active_env=staging && alembic upgrade head && gunicorn -w 4 -k uvicorn.workers.UvicornWorker  veriprops:app",
  "builds": [
    {"src": "/veriprops.py", "use": "@vercel/python"}
  ],
  "routes": [
    {"src": "/(.*)", "dest": "veriprops.py"}
  ]
}
