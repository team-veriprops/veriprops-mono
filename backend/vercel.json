{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "installCommand": "uv pip install --system -r requirements.txt",
  "buildCommand": "export veriprops_active_env=prod && alembic upgrade head",

  "functions": {
    "api/index.py": {
      "maxDuration": 60,
      "includeFiles": "service_accounts/**"
    }
  },

  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
