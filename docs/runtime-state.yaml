# Orchestrator runtime state — cycle 2 (WhatsApp Channel, PRD.md §7)
# Cycle-1 state archived at docs.back/runtime-state.yaml.

session_id: null
command: run

status: idle
phase: idle

current_slice: null
current_subtask: null

modified_files: []
created_files: []

tests:
  written: true
  executed: true
  passing: true

self_audit:
  started: true
  completed: true
  findings:
    - "S4.1 gates green: backend 1283 unit tests, ruff + mypy clean; frontend 515 unit tests,
       eslint clean, next build ok. Full drive-through (all 18 stages): ALL PASSED."
    - "Migration 0005 verified three ways against live Postgres — additive on an already-migrated
       schema, full chain on a fresh database, and a downgrade/re-upgrade round trip; the in-place
       result is column-for-column identical to the fresh chain."
    - "S4's deferred gap is closed (D59). All seven §7.7 templates are declared with bodies and
       ordered parameters; `otp_auth` now sends as a real Meta template rather than free text,
       which is what the Cloud API requires outside the 24-hour window — the exact case a linking
       OTP to a cold number always is."
    - "Registry split on the line that has an owner: definitions in code (the code fills the
       parameters), approval status synced from Meta through a WHATSAPP_PROVIDER-selected
       directory facade. Admin page at /admin/config/whatsapp-templates, read-only by design."
    - "Fixed: `_build_template` ordered Meta's positional body parameters as strings, so \"10\"
       preceded \"2\" — harmless at today's counts, silently wrong past nine parameters."
    - "Fixed: `MessageContext.BRAND` was fed `settings.BRAND`, a lowercase *slug* documented for
       log lines. Every customer message read 'veriprops: …', including the linking OTP — the one
       message §7.1.2's anti-impersonation posture depends on. Renamed REPORT_BRAND_NAME to
       BRAND_DISPLAY_NAME and injected it; one display name now serves reports and every template."
    - "Fixed: the /dev/whatsapp/outbox endpoint serialised with a bare model_dump(), so it alone
       answered in snake_case against the repo's camelCase wire contract — an assertion written to
       the contract read a working template send as free text. Found by that assertion failing."
    - "Fixed: four orphan template files (email/sms/whatsapp `otp.jinja2`, push
       `new_house_listing.jinja2`) had no AvailableTemplate entry and a stale variable vocabulary.
       Deleted, with a guard test that every template file maps to an enum entry — the reverse of
       the existing rule, so dead copy cannot accumulate again."
    - "Fixed: `tsc --noEmit` — a CI gate — had been red since S1/S3 on eight errors in two test
       files, invisible locally because neither `pnpm lint` nor `pnpm build` typechecks tests.
       A `ROUTES.HOME` initialiser narrowed a variable to the literal \"/\", and an HttpClient mock
       was missing `getBlob`. Both fixed, and `pnpm typecheck` added so the gate is runnable
       outside CI."
    - "Fixed: the ops_unhappy sweep checks asserted this caller's reclaim count, which the dev
       server's own 15-minute APScheduler sweep can consume seconds earlier. They now assert the
       outcome (task back to PENDING with a strike, task out of the pool) — the property under
       test is that an unaccepted assignment gets reclaimed, not who reclaimed it."
    - "CLOSED (D61): the authentication-template button is wired. Meta mandates an OTP button on
       authentication templates, so `otp_auth` could not have been delivered without it. Meta's own
       send-side pages 404, but four BSP docs that mirror their contract agree on the shape
       (sub_type url, index \"0\", the code as the single parameter), and copy-code and one-tap
       send identically. The stub records the button parameter, so a stub run proves a component
       the live send requires. Remaining: confirm the approved template matches, at the S11 smoke."
    - "Deferred by design: six of the seven templates have no sender yet — milestones and report
       delivery at S8, `window_reopen` at S7 (WhatsAppWindowService already landed),
       `delegate_status` at S9. Declaring them early is the point of §7.7."
    - "CLOSED (D60, amending D46): the §7.4.4 SMS fallback is wired. D46 deferred it on the unmade
       §B *provider* decision, but MessageRouter's sms channel already encoded the whole chain —
       MOCK_SMS exclusively in test/dev, Termii → Twilio for +234 in prod. The decision had been
       made in the router and nothing was reading it. WhatsApp first, SMS to the same number when
       that fails; one code under one key, so verification is unaffected by which transport won."

clarification_needed: []

git:
  branch: dev
  dirty: false

next_action: "await review of S4.1, then `run` — begin S5 (bot engine core) per docs/execution-plan.md"

started_at: "2026-08-31"
last_checkpoint: "2026-09-01 S4.1 + follow-up (auth button D61, SMS fallback D60, tsc gate), full drive-through ALL PASSED"
